#!/bin/sh

wall=$(find ~/pix/walls/ -type f -name "*.jpg" -o -name "*.png" | shuf -n 1)

feh --bg-fill $wall &

betterlockscreen -u $wall &

cp ~/.cache/wal/dunstrc ~/.config/dunst
cp ~/.cache/wal/polybar-config.ini ~/.config/polybar/config.ini

wal -c
wal -i $wall
killall dunst
dunst
